# --- Secrets and credentials (never commit) ---
.env
.env.*
*.pem
*.key
*.p12
*.pfx
secrets/
credentials/
*credentials*.json

# --- Local pi agent runtime ---
.pi/

# --- Entire local/session data (not the shareable project settings) ---
# Keep .entire/settings.json trackable; ignore only local-only Entire files.
.entire/settings.local.json
.entire/sessions/
.entire/logs/
.entire/cache/
.entire/*.local.*

# --- OS artifacts ---
.DS_Store
Thumbs.db
desktop.ini

# --- Build outputs and local fixture profiles ---
node_modules/
dist/
/target/
**/target/
coverage/
playwright-report/
test-results/
*.tsbuildinfo
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal
.email-os/
/src-tauri/gen/
/mise.local.toml
